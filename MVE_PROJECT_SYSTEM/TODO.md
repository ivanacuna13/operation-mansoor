# MVE Project System — TODO

Mirror of `PROGRESS.md`. Update status when work moves.

Legend: Done / In progress / Blocked / Pending

## Now / next

1. **Done** — `01_INVENTORY.md`
2. **Done** — Align MASTER + dispatch SKILL + MVE profile to live **4**-type registry + `#sf-video-ready-to-review`
3. **Done** — Freeze reaction channel allowlist in `mve.py` (`07_SLACK_IDENTITY.md`); lifestyle still ignored
4. **Done** — `project_layout.py` + `revision_identity.py` + schema copy + `AGENTS_PROJECT_RULES.md`; `init-layout` for new jobs
5. **Done** — Contracts `02`–`06` drafted; `08_CHANGES.md` for foundation pass
6. **Done** — Wire success_layers into delivery-gate / `revision_completeness_gate.py` (fail-closed if missing)
7. **Done** — Point each format skill at `AGENTS_PROJECT_RULES.md` without rewriting approved edits
8. **Done** — Kill/quarantine self-attested QC flags; shared Frame stack-head helper
9. **Done** — Harden model path (Grok fallback off if config missing); log model/effort (`worker-model.json` + audit)
10. **Done** — Batch stub: `analysis_cache_key` + advisory single-writer lock (`batch_ownership.py`) — Premiere not wired
11. **Blocked** — Premiere/AME on Mini (no SSH this pass)
12. **Pending** — Pilot: one scripted batch + multi-clip sales call on new layout with success_layers + stack-head resolve
13. **Pending** — Workers must emit `success_layers` + evidence in `final-result.json` (prompted; not yet proven on a live job)
14. **Done** — Wire `revision_identity` pointers into live revision write path (`record_revision_attempt` / `mark_revision_approved` / `identity-status`)
15. **Pending** — Genius/copycat validators: confirm no remaining self-attested pass aggregators beyond skill pointer

## Done
- [x] Copy `SOURCE_SPEC.md` + progress scaffolding
- [x] Confirm 4 skill files exist
- [x] Factual inventory
- [x] Docs/profile 4-type + channel align
- [x] Reaction allowlist (sf-video / content-team / legacy ai-content-team; reject lifestyle)
- [x] New layout + revision identity library (non-destructive)
- [x] Frame stack-head helper + success_layers gate + attestation quarantine + skill pointers + fallback harden + batch stub
- [x] Live revision_identity write path + identity-status CLI + unit tests

## Blocked (explicit)
- Premiere/AME live bridge on Mini — paused
- Lifestyle Copycat MVE reaction automation — intentionally Eddie-owned; keep ignored by MVE `cmd_reaction`

## Pilot (2026-09-08)
16. **Done** — Synthetic identity/gate dry pilot (`09_PILOT_STATUS.md`, `pilot-synthetic/`)
17. **Pending** — Live Codex+Frame pilot on a **new** assigned job only
